const batches = require('./batches')
const sessions = require('./sessions')
const users = require('./users')
const students = require('./students')

module.exports = {
  batches, users, sessions, students
}
